{
  "name": "valor-theme",
  "version": "1.6.1",
  "private": true,
  "scripts": {
    "check": "shopify theme check",
    "check:json": "shopify theme check --output json",
    "format": "prettier . --write",
    "format:check": "prettier . --check",
    "package": "shopify theme package"
  },
  "devDependencies": {
    "@shopify/prettier-plugin-liquid": "^1.10.3",
    "prettier": "^3.8.4"
  }
}
